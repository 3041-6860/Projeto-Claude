# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

This project is in the planning/initialization phase. No code has been written yet. Project documentation is in Portuguese and resides in `Projeto Claude.docx` and `Projeto Claude.pdf`.

## RTK (Token Optimization)

This environment has RTK configured globally. Prefix commands with `rtk` for token savings:

```bash
rtk git status
rtk git log
rtk git diff
```
