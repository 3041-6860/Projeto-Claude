name: Deploy Inove Prime
on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 170.187.131.141
          username: root
          password: ${{ secrets.VPS_PASS }}
          port: 22
          command_timeout: 10m
          script: |
            cd /root/inove-deploy
            git pull origin main
            bash b
